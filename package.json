{
  "name": "mediscan",
  "version": "0.1.0",
  "private": true,
  "description": "MediScan AI - Frontend + Backend convenience scripts",
  "scripts": {
    "serve-frontend": "npx http-server ./Frontend -p 8000",
    "start-backend": "python backend/app.py",
    "start": "npm run serve-frontend",
    "install:lock": "node -e \"console.log('Run npm install to generate package-lock.json')\""
  },
  "dependencies": {
    "@amcharts/amcharts5": "^5.0.0",
    "jspdf": "^2.5.1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
